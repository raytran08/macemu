# cross-built m68k probe binaries; rebuild with the toolchain in the
# nbwork docker volume, they are not portable and not worth carrying
fbcoexist
cmapprobe
BasiliskII
wedgebisect
mapnotifytest
cmaptest
fbshot
screen.ppm*
s[0-9].ppm*
